# Báo cáo công việc: trích dẫn [n] + link tài liệu tham khảo

**File kết quả:** `~/Downloads/CĐTN_A46489_DOANNAMANH_F2T_CITED.docx` (duplicate từ file thân bài; bản gốc giữ nguyên).

## Bước 1 — 37 tài liệu tham khảo
- **Tìm URL cho cả 37 mục → không xoá mục nào** (chi tiết URL + tóm tắt: `references_report.md`).
- Mỗi mục được **đặt bookmark `Ref_n`** và **gắn link "(nguồn)"** ở cuối, trỏ tới trang nguồn thật (Ctrl+Click mở).
- [33][34][35] tra qua web search (IJACSA 2023 / Springer SDSC 2025 / J. Cleaner Production 2022). Paper kinh điển & tài liệu chính thức dùng URL chính tắc của tác giả/NXB.

## Bước 2 — chèn trích dẫn [n] trong bài
- **48 trích dẫn `[n]`** đã chèn tại vị trí `[TLTK]` cũ, dạng **hyperlink nội bộ** → Ctrl+Click nhảy tới đúng mục trong "TÀI LIỆU THAM KHẢO".
- **30/37 tài liệu được trích dẫn** trong bài.

### Các [TLTK] KHÔNG map (đúng như thoả thuận "không rõ thì bỏ")
- **14 cái chung chung** (không có nguồn trong 37 mục): định nghĩa TMĐT/F2T, taxonomy "4 nhóm AI trong TMĐT" (recommender 20–35%, demand, dynamic pricing, visual AI), chênh giá 30–50%, NFR/môi trường dev.
- **8 cái không đặt được trong bản docx này:**
  - `[27]` Sutton&Barto: chỗ trích là dòng **công thức LaTeX** (Bellman) — không khớp text.
  - `[18]` NestJS (1 vị trí lệch chữ) — **nhưng [18] vẫn được trích ở câu khác**, không mất.
  - `[33][34][35]` Nassibi/Xue/Kayikci: chỗ trích nằm ở **Bảng 4.10 (so sánh quốc tế) + đoạn §4.4** vốn **không có trong bản docx** này (bản docx khác draft với file md / bảng đã chuyển sang phụ lục).

### 7 tài liệu "mồ côi" (có trong danh mục, chưa có [n] trong bài)
`[19]` Node.js, `[23]` Uvicorn, `[24]` Flask — chỉ xuất hiện trong **bảng thư viện (Bảng 4.2–4.4)** nay đã ở **file phụ lục**.
`[27]` (dòng công thức), `[33][34][35]` (§4.4 không có trong docx).
→ Đây là hệ quả của việc bản docx khác draft md + bảng đã tách sang phụ lục, **không phải lỗi map**. Có thể bổ sung thủ công nếu muốn trích ở phụ lục (sẽ là link chéo file).

## Cách dùng trong Word
- **Ctrl+Click `[n]`** trong bài → nhảy tới mục `[n]` ở Tài liệu tham khảo.
- **Ctrl+Click "(nguồn)"** ở mục tham khảo → mở trang nguồn thật trên web.

## Lưu ý nhỏ
- Một vài chỗ hiển thị `... [n] .` (dư một khoảng trắng trước dấu chấm) do vị trí `[TLTK]` cũ — thuần mỹ thuật, có thể tinh chỉnh sau nếu cần.
